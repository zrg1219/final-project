require_relative 'lib/twilio.rb'


task :no_lazy do
  #create a new instance of your texts class
  #call the send_texts method on that instance
  text = Texts.new
  text.send_text("+19149806396")
end

